@model MerchStore.WebUI.Models.Catalog.ProductDetailsViewModel

<div class="container mt-4">
    <div class="row">
        <div class="col-md-6">
            <img src="@Model.ImageUrl" class="img-fluid" alt="@Model.Name" />
        </div>
        <div class="col-md-6">
            <h2>@Model.Name</h2>
            <p>@Model.Description</p>
            <p><strong>Pris:</strong> @Model.FormattedPrice</p>
            <p><strong>Lagerstatus:</strong> @(Model.StockQuantity > 0 ? $"I lager ({Model.StockQuantity})" : "Slut i lager")</p>

            <form method="post" asp-controller="Cart" asp-action="AddToCart">
                <input type="hidden" name="id" value="@Model.Id" />
                <input type="hidden" name="name" value="@Model.Name" />
                <input type="hidden" name="price" value="@Model.PriceAmount" />
                <label for="quantity">Antal:</label>
                <input type="number" name="quantity" id="quantity" value="1" min="1" class="form-control mb-2" />
                <button type="submit" class="btn btn-success w-100">🛒 Lägg till i kundvagn</button>
            </form>

            <div class="mt-3 d-flex gap-2">
                <a asp-controller="Cart" asp-action="Index" class="btn btn-success">Gå till kassan</a>
                <a asp-controller="Catalog" asp-action="Index" class="btn btn-secondary">Fortsätt handla</a>
            </div>

        </div>
    </div>
</div>
